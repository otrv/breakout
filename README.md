# breakout

A minimal breakout clone in C using [raylib](https://www.raylib.com/).

## Build

```sh
cc main.c -o breakout -lraylib -lm
```

## Run

```sh
./breakout
```

## Controls

- `A` / `D` — move paddle
- `ESC` — quit
